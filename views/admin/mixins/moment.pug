mixin formatDateTime(date)
    span #{moment(date).format('DD/MM/YYYY HH:mm:ss')}

mixin formatDate(date)
    span #{moment(date).format('DD/MM/YYYY')}

mixin formatTime(date)
    span #{moment(date).format('HH:mm:ss')}
