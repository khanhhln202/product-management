include ../mixins/sub-menu.pug

header.header 
    .container
        .row.align-items-center 
            .col-3 
                .inner-logo 
                    a(href="/")
                        img(src="/images/LumenPathLogo01.png", alt="Logo")
            
            .col-9 
                .inner-menu 
                    ul 
                        li 
                            a(href="/") Home page
                        li(class="sub-menu")
                            - const pathProduct = "/products"
                            a(href=pathProduct) Products
                            +sub-menu(layoutProductCategories, pathProduct)

                            